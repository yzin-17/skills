# Mockoon 独立随机空数据响应实施计划

## 实施步骤

1. 调整成功场景构建：始终以正常渲染生成 `success-default` 与 `success-list-*`；仅在 `randomEmptyData` 开启时追加 `success-random-empty`。
2. 调整场景重建识别规则，使重新渲染能替换新增场景，同时继续保留人工场景。
3. 更新单元与 CLI 端到端测试，验证默认场景不再包含空态模板、新场景包含空态模板、关闭开关时不生成该场景。
4. 更新设计相关说明，执行测试、类型检查、构建、bundle 和打包 CLI 检查。
